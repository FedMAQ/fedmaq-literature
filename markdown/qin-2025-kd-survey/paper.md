Wiley International Journal of Intelligent Systems Volume 2025, Article ID 7406934, 33 pages https://doi.org/10.1155/int/7406934

## Review Article

## Knowledge Distillation in Federated Learning: A Survey on Long Lasting Challenges and New Solutions

<!-- image -->

Laiqiao Qin , 1 Tianqing Zhu , 1 Wanlei Zhou , 1 and Philip S. Yu 2

<!-- image -->

Correspondence should be addressed to Tianqing Zhu; tqzhu@cityu.edu.mo

Received 1 April 2025; Revised 4 September 2025; Accepted 15 October 2025

Academic Editor: Richard Murray

Copyright © 2025 Laiqiao Qin et al. International Journal of Intelligent Systems published by John Wiley &amp; Sons Ltd. This is an open access article under the terms of the Creative Commons Attribution License, which permits use, distribution and reproduction in any medium, provided the original work is properly cited.

Federated learning (FL) is a distributed and privacy-preserving machine learning paradigm that coordinates multiple clients to train a model while keeping the raw data localized. However, this traditional FL poses some challenges, including privacy risks, data heterogeneity, communication bottlenecks, and system heterogeneity issues. To tackle these challenges, knowledge distillation (KD) has been widely applied in FL since 2020. KD is a validated and efficacious model compression and enhancement algorithm. The core concept of KD involves facilitating knowledge transfer between models by exchanging logits at intermediate or output layers. These properties make KD an excellent solution for the long-lasting challenges in FL. Up to now, there have been few reviews that summarize and analyze the current trend and methods for how KD can be applied in FL efficiently. This article aims to provide a comprehensive survey of KD-based FL, focusing on addressing the above challenges. First, we provide an overview of KD-based FL, including its motivation, basics, taxonomy, and a comparison with traditional FL and where KD should execute. We also analyze the critical factors in KD-based FL in the Appendix, including teachers, knowledge, data, and methods. Wediscuss how KD can address the challenges in FL, including privacy protection, data heterogeneity, communication efficiency, and personalization. Finally, we discuss the challenges facing KD-based FL algorithms and future research directions. We hope this survey can provide insights and guidance for researchers and practitioners in the FL area.

Keywords: communication efficiency; federated distillation; federated learning; knowledge distillation; non-IID; personalization; privacy preservation; system heterogeneity

## 1. Introduction

Federated learning (FL) is a distributed collaborative machine learning paradigm to protect data privacy, McMahan et al. [1]. FL enables participants to build a robust and generalized federated model collaboratively through periodic local updates and communication without sharing raw data. Traditional FL algorithms, such as FedAvg, McMahan et al. [2], involve multiple rounds of client communication for repeatedly averaging local model parameters under server coordination. In each round, clients receive the global model from the server to initialize their local model, update their local model using private local data, and upload the updated local model to the server. The server aggregates the received local models to update the global model and then broadcasts the updated model parameters to the clients. The entire process is repeated until the global model converges.

However, this traditional parameter-based paradigm of FL may have some long-lasting challenges. First, sharing model parameters (or gradients) may compromise data privacy, Li et al. [3]. For example, adversaries could use these to reconstruct the raw data, Geiping et al. [4]. Second, clients' data are mostly not non-IID, Zhao et al. [5], causing local models to suffer from client drift, Karimireddy et al. [6], and deviate from the global optimization objective, resulting in slow convergence, Wang et al. [7], and degraded model performance, Zhao et al. [5]. Third, the frequent uploading of local models can lead to a severe communication bottleneck, Li et al. [3]. Lastly, parameter-based algorithms lack support for heterogeneous model architectures, Li and Wang [8], personalization for clients, Tan et al. [9], and efficient handling of system heterogeneity, Ozkara et al. [10].

<!-- image -->

<!-- image -->

<!-- image -->

<!-- image -->

Tackling the above long-lasting challenges in the traditional parameter-based FL is not easy. There are four key reasons that hinder the development of the FL: (1) Since data cannot be shared directly between clients, the clients' model updates lack global awareness, Karimireddy et al. [6]; Varno et al. [11]. Traditional FL hopes to solve this problem by aggregating the model parameters of each client. (2) However, model parameters are only the results of client training and not the statistical characteristics of the data. This will lead to deviations in the aggregation model in nonIID scenarios, Li et al. [12]. (3) The premise of the current mainstream machine learning algorithms is IID, which is often not true in FL, Huang et al. [13]. (4) The information in the model parameters is too rich and redundant, far exceeding the minimum amount required for collaborative training, leading to privacy, Mothukuri et al. [14], and communication, Hamer et al. [15], issues.

To fill the gaps, knowledge distillation (KD), Hinton et al. [16], has been widely applied in FL, Lin et al. [17], since 2020. KD is a highly effective model compression and enhancement technique involving transferring knowledge from a high-performance teacher to a student model, Phuong and Lampert [18]; Mirzadeh et al. [19]. Figure 1 is the basic process of KD, which shows a teacher-student architecture. The teacher network outputs soft labels for the same training data, and the student network uses these soft labels as regularization constraints to learn from the teacher model, Hinton et al. [16]. Instead of directly copying the parameters of the teacher model, the student model emulates the soft labels of the teacher model to achieve knowledge transfer. This distinctive approach to transferring knowledge in KD reduces communication costs, Sattler et al. [20], and allows for different architectures between teacher and student models, Ozkara et al. [10]. By the teacher model's soft labels, the student model is endowed with regularization constraints that facilitate superior generalization capabilities.

This typical teacher-student framework in KD is quite suitable for FL. KD-based FL algorithms only require clients to exchange the logits of their local models, Chang et al. [21], without uploading model parameters, thus significantly reducing potential privacy risks and communication costs. Unlike parameter-based methods that exchange in the parameter space, KD-based methods exchange in the function space, Taya et al. [22]; Li et al. [23]. This allows for the preservation of personalized knowledge between different models to some extent in non-IID scenarios rather than being directly flattened or eliminated, Xiao et al. [24]. This process of functional approximation, rather than structural replication, fundamentally alters the privacy-utility trade-off in FL. Furthermore, KD is model-agnostic, Hinton et al. [16], and clients have the flexibility to create personalizationfocused models with varying architectures, Huang et al. [25]; Xie et al. [26].

Many algorithmic solutions, Zhang et al. [27]; Wang et al. [28], based on KD have been proposed to tackle the above challenges in FL. These solutions can be roughly divided into three categories: (1) feature-based FD, (2) parameter-based FD, and (3) data-based FD. Feature-based FD algorithms transmit only the model's features between clients and the server, such as logits, Jeong et al. [29]; Huang et al. [30], attention, Gong et al. [31]; Wen et al. [32], and intermediate features, Shi et al. [33]. Since these features are much smaller and contain far less sensitive information than model parameters, they can effectively address the communication bottleneck, Sattler et al. [20], and privacy risks, Gong et al. [34], in FL, as well as personalize requirements by using different model architectures, Liu et al. [35]. Parameter-based FD algorithms share model parameters between clients and the server. This method is primarily used to address the issue of model performance degradation caused by directly aggregating model parameters in non-IID scenarios, Tang et al. [36]; Nguyen et al. [37]. Data-based FD algorithms require clients to use dataset distillation, Wang et al. [38], methods to compress their data into a small-scale dataset, Zhou et al. [39]; Song et al. [40], which is not widely adopted in FL, and this survey will not delve into this approach further. Table 1 summarizes which challenges these three KD-based FL methods solve in FL.

Even though KD-based FL has shown great potential to resolve the long-lasting challenges in FL, the area remains in its early stages, and the associated research exhibits considerable breadth and diversity. The problems and corresponding solutions targeted by these KD-based algorithms are not identical, and the description of 'KD in FL' cannot fully summarize them. Different literature has diverse perspectives, explanations, and specific implementations. For instance, while Jeong et al. [29] and Li and Wang [8] both use the average value of clients' logits as knowledge, Jeong et al. [29] uses local private data, whereas Li and Wang [8] use public data. Likewise, although Sattler et al. [43] and Lee et al. [44] are both based on FedAvg, Sattler et al. [43] uses the local model as a teacher, whereas Lee et al. [44] uses the global model as a teacher. A comprehensive analysis of these methodologies can accelerate further application of this area.

Our primary focus is on the inherent critical factors of KD-based FL algorithms and how KD addresses the longlasting challenges of FL. The main contribution of this paper is to analyze the fundamental reasons why KD can resolve the long-lasting challenges of FL. We also analyzed some characteristics of using KD in FL and proposed a taxonomy. The details are summarized as follows:

1. We provide a systematic analysis of KD-based FL, summarizing the differences between KD-based FL and parameter-based FL algorithm paradigms and analyzing where KD should execute. By comparing the knowledge transfer methods used by different algorithms, we classify KD-based FL algorithms into three categories: feature-based FD, parameter-based FD, and data-based FD.
2. We summarize the critical factors related to state-ofthe-art KD-based FL algorithms, including the role of teachers, types of knowledge, dataset acquisition for distillation, and specific methods employed. Details

ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License can be found in the appendix. These factors must be considered by all KD-based FL algorithms. By analyzing these factors, we provide important references for future research in KD-based FL.

Figure 1: The typical training process of knowledge distillation.

<!-- image -->

Table 1: KD-based FL methods are used to solve challenges in FL.

|                                   | Privacy   | Non-IID   | Communication   | Personalization   |
|-----------------------------------|-----------|-----------|-----------------|-------------------|
| Feature-based, Li and Wang [8]    | ✓         | ✓         | ✓               | ✓                 |
| Parameter-based, Shen et al. [41] |           | ✓         |                 |                   |
| Data-based, Zhou et al. [42]      |           | ✓         | ✓               |                   |

3. We provide a comprehensive introduction to the application of KD in solving various challenges in FL. These challenges include privacy preservation, mitigating slow convergence and model performance degradation in data heterogeneous scenarios, reducing communication bottlenecks, and addressing personalized model requirements and system heterogeneity issues resulting from variations in client network and hardware resources.
4. We discuss the challenges facing KD-based FL and future research directions.

## 2. Background

2.1. FL. FL enables multiple clients to collaboratively train a global model with the coordination of a central server. Suppose there are N independent clients, the matrix Di represents the local data of client i , where i ∈ [ 1 , N ] . Each client has a different local model wi and objective function fi ( w ) . Each client is given a different weight q i . The goal of FL is to obtain a generalized global model wg through multiple rounds of local training and global aggregation. w r i represents the local model of client i in the r -th round of communication, and w r g represents the global model in the r -th round. The definition and training process of FL is as follows.

Definition 1 (see Yang et al. [8]). In FL settings, client i aims to achieve the learning objective by iteratively updating their local models through aggregating model results from multiple clients without exchanging or transmitting their local data Di and by sharing model parameters or outputs, Kairouz et al. [45]. This process often involves the assistance of a central server, but it is not necessary for certain scenarios or system architectures.

Training process: As shown in Figure 2, the typical training process for FL is as follows:

1. Model distribution: In round r , the central server distributes the current global model w r g to each client. At the beginning, the global model w 0 g can be randomly initialized.
2. Local model update: The client i initializes the local model with the global model w r g and trains it with local data Di . When obtaining a new local model w r + 1 i , the client sends it to the central server.
3. Global model update: The central server aggregates the local models w r + 1 i to obtain a new global model w r + 1 g . Then, the process returns to Step 1. The entire process is repeated until convergence.

2.2. KD. KD is an effective method for model compression and enhancement, Wang and Yoon [46]. The main idea is to transfer knowledge from a high-performing teacher model to a student model, as shown in Figure 3. In KD, knowledge transfer is accomplished by having the student model mimic the soft labels of the teacher model instead of its parameters, Phuong and Lampert [18]; Gou et al. [47]. This method of knowledge transfer in KD reduces communication costs and permits the student model architecture to differ from that of the teacher model. The soft labels provided by the teacher model serve as regularization constraints for the student model, Yuan et al. [48]; Mora et al. [49], which helps improve its generalization performance.

Definition 2. In the setting of KD, there are two models: the student model S and the teacher model T . S is the model to be trained. By imitating the output of T on the dataset D , S can improve its performance. Let S 1 be the model S trained solely on D without guidance from T , and S 2 be the model S trained on D with guidance from T . Let Vt , Vs 1 , and Vs 2 be the performance measures of T , S 1 , and S 2 , respectively. Then,

Figure 2: The typical training process of federated learning consists of ① model distribution, ② local model update, and ③ global model update.

<!-- image -->

Figure 3: KD uses a teacher-student architecture. The teacher model transfers knowledge to the student model.

<!-- image -->

$$
\left | V _ { s _ { 2 } } - V _ { s _ { 1 } } \right | < \delta , \left | V _ { t } - V _ { s _ { 2 } } \right | < \eta ,
$$

where δ and η are non-negative real numbers that satisfy the above equation as small as possible. At this point, we say that Vs 2 has a performance gain of δ , which can be seen as the result of the student model learning from the teacher model. Typically, Vt &gt; Vs 2 due to the capacity difference between the teacher and student models. However, when the complexity of the student model is equivalent to or even exceeds that of the teacher model, Vs 2 &gt; Vt may occur.

Training process: Assuming there is a pretrained teacher model T, as shown in Figure 1, the typical training process of KD is as follows:

1. The teacher network outputs soft labels.
2. The student network uses the soft labels from the teacher network as a regularization constraint for the loss function during training. The entire process is repeated until convergence.

Usually, we use softmax to convert the logits, z i , into class probabilities, q i , which are used as soft labels, Hinton et al. [16],

$$
q _ { i } = \frac { \exp \left ( z _ { i } / \tau \right ) } { \sum _ { j } \exp \left ( z _ { j } / \tau \right ) } ,
$$

where temperature τ is a hyperparameter, and higher values of τ will produce smoother output probability distributions, which will make the model pay more attention to negative labels, and the student model can learn more knowledge from the teacher model, Hinton et al. [50]; Tang et al. [51]. A special case is to directly match logits, which is equivalent to τ ⟶ + ∞ .

2.2.1. Long-Lasting Challenges in FL. Due to its unique setup, FL presents several distinct challenges compared to traditional centralized training.

Privacy preservation: The original intention of FL was to protect data privacy, but research has shown that sharing only client-side gradients can reveal private data, Li et al. [3]; Mothukuri et al. [14]. In fact, model parameters such as weights or gradients can be viewed as a 'compression' of the dataset, which can be restored using certain techniques, Geiping et al. [4]; Huang et al. [52]. This creates a conflict, where the sharing of intermediate results is necessary to train a global model, but at the same time, sharing these results also poses a risk to data privacy. Thus, there is a need to balance the compression of intermediate results and data privacy.

Data heterogeneity: An essential assumption in machine learning is that the dataset is IID. However, this assumption does not hold in FL. The datasets of various clients not only differ in size but are also often not identically distributed. Each client may have completely different model updates, causing the local objective to deviate entirely from the global objective, resulting in global knowledge forgetting and client drift, Yashwanth et al. [53]. This slows down model convergence and degrades model performance, Wang et al. [7]; Zhang et al. [54].

Communication bottleneck: To achieve better model performance, participants in FL often use larger models with improved performance, Allen-Zhu et al. [55]; Zhou [56]. However, training a large model collaboratively can increase the communication cost between clients and the server when exchanging model parameters. During FL training, there are often many rounds of local training and global aggregation, which require frequent uploading of local models and downloading of global models. In many scenarios, this creates a severe communication bottleneck (e.g., on mobile and edge devices), making FL impractical, Koneˇ cn ỳ et al. [57].

Model personalization: For cross-device FL, clients typically use the same model. However, in many crosssilo scenarios, it is a reasonable and practical requirement for each client to design personalized models based on their dataset and specific application scenarios, Wang et al. [28]; Singh et al. [58]. Currently, the mainstream methods in FL are based on parametersharing, which does not support heterogeneous models because the knowledge of different models is not compatible in form, and a personalized model cannot directly learn the knowledge of other models through parameters.

2.3. KD-Based FL. KD-based FL is an emerging paradigm that leverages KD to aggregate the features of client-side models and facilitate collaborative model training. The critical distinction between KD-based FL and parameterbased FL lies in their divergent definitions of knowledge. KD-based FL considers various aspects of the model's features, Gou et al. [47], such as output layer features, Chang et al. [21]; Li and Wang [8], and intermediate features, Gong et al. [31]; Yang et al. [59], as knowledge. In contrast, parameter-based FL considers the model's parameters (or model gradients) as knowledge. The two methods exhibit distinct characteristics and face unique challenges within the same privacy protection framework in FL.

1. The models communicate through the transmission of dark knowledge, Hinton et al. [16], which is based on features instead of parameters.
2. KD-based FL needs a dataset for distillation, which can be client private data, Jeong et al. [29], publicly available data, Chang et al. [21], or artificially generated synthetic data, Zhang et al. [27].
3. Typically, KD-based FL lacks a pretrained teacher model, Wang et al. [60], and the initial training performance of the teacher model is suboptimal. However, the teacher model gradually improves reliability and convergence as the training progresses.

KD-based FL is a concept that lacks a precise definition. Some studies, Sattler et al. [61]; Xing et al. [62], refer to specific KD-based FL algorithms as federated distillation (FD), but the meaning of FD varies across papers. In this paper, KD-based FL refers to utilizing KD for addressing specific challenges in FL during local training or server aggregation stages. This paper does not differentiate between KD-based FL and FD concepts to avoid confusion. Section 3.3.3 will discuss a distinct type of FD that employs dataset distillation, Wang et al. [38], instead of KD. In this sense, the term FD better represents the discussed concept than KD-based FL.

In the early stages of KD-based FL research, the primary focus was on minimizing communication costs, Jeong et al. [29]; Ahn et al. [63], where clients would upload their local model outputs to the server for aggregation. Subsequently, KD evolved to tackle various challenges in FL and was integrated with parameter-based FL. Figure 4 illustrates the comprehensive training process of FL, where KD can be employed at all six steps.

## 3. KD-Based FL

3.1. KD-Based FL: Motivation. In the context of FL, data cannot leave the local device, and collaborative model training is accomplished through periodic exchange of model information among clients, Yang et al. [64]. Therefore, the model information exchanged in FL must satisfy specific requirements: (1) the model information must be sufficiently abstract to prevent exposure of raw data privacy, (2) the model information must be sufficiently representative to facilitate the collaborative training of a highperformance generalized model by clients, and (3) the model information must be compact enough to minimize communication costs. Traditional FL algorithms utilize model parameters (or parameter gradients) as the model information, McMahan et al. [1,2], to be exchanged among clients to achieve the learning objective of FL.

However, related research, Geiping et al. [4]; Zhu et al. [65], has shown that model parameters may not be an ideal model information in collaborative learning. Sharing parameters or gradients may lead to the potential disclosure of sensitive original data, Huang et al. [52]; Lam et al. [66]. Exposing the complete model parameters to potential attackers poses a significant security threat, Yin et al. [67]. Although some research, Mothukuri et al. [14]; Yin et al. [67], has been proposed to compensate for the shortcomings of sharing model parameters, inherent defects are challenging to overcome. The concern about privacy in parameter-sharing algorithms is because the information exchanged is too rich and redundant.

In the training process of parameter-based FL, the primary challenge is the communication bottleneck, Shahid et al. [68]; Gad et al. [69]. In the context of FL, clients are distributed, and their communication can be unstable, slow, asymmetric, and expensive. Frequent transmission of complete model parameters results in unbearable communication overhead, thereby limiting the participation of potential clients, particularly in scenarios involving mobile or edge devices. In addition, uploading model parameters slows down communication and prolongs the training process. Moreover, participants may seek to collaborate on training high-performance and large models, significantly augmenting the communication costs. Although some work, Luping et al. [70]; Sattler et al. [71], has been proposed to mitigate communication overhead, overall communication costs are related to model size.

Participants in FL exhibit diverse identities and goals, working collaboratively to achieve their respective objectives. However, parameter-based FL algorithms impose the constraint of utilizing a uniform model across all participants to attain their objectives, which is impractical in realworld scenarios, Mansour et al. [72]; Kulkarni et al. [73]. For instance, in the case of company participation, there may be a desire to acquire a high-performance and large model; however, such a model might not be suitable for certain participants aiming to deploy it on low-performance devices. Parameter-based algorithms fail to address the personalized modeling requirements of clients. Moreover, sharing model parameters fails to address the computational performance and network connectivity disparities arising from system heterogeneity, Luo et al. [74]; Chen et al. [75].

The most significant obstacle in FL is data heterogeneity, Zhao et al. [5]; Zhu et al. [76]. The objective of participant collaboration is to obtain a more robust and generalizable model compared to the one solely trained on local data. The performance of the global model might be compromised by data heterogeneity, resulting in a decline in participants' propensity to collaborate. The stochastic gradient descent method, Amari [77], is widely employed in machine learning as the primary optimization technique, assuming that the training data follows an IID pattern. However, the data ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License distribution of each client in FL is often non-IID, Zhu et al. [76], resulting in model parameters that solely reflect the optimization outcomes of local private data and fail to capture the optimization results across all data. The optimization directions of different client model parameters exhibit significant variations, and a simple averaging approach fails to yield the globally optimal model. Furthermore, conventional parameter-based algorithms assign different weights to model updates of each client, prioritizing participants with larger datasets while neglecting updates from smaller datasets, leading to fairness concerns in FL, Huang et al. [78]; Li et al. [79]. In addition, non-IID data distribution presents challenges for model convergence, Wu and Wang [80]; Wang et al. [7], particularly in parameterbased algorithms where direct averaging of model parameters fails to capture the underlying knowledge derived from updates contributed by diverse clients.

Figure 4: The FL process consists of six steps: ① preprocessing of the global model by clients, ② local training by clients, ③ further processing of the local models by clients, ④ preprocessing of the local models by the server upon receiving them, ⑤ aggregation of the local models by the server to obtain the global model, and ⑥ further processing of the global model by the server. KD can be employed in all six steps.

<!-- image -->

Therefore, FL urgently requires alternative methods for exchanging model information. KD-based FL adopts KD to exchange the logits of the local model to transmit model information between clients, Jeong et al. [29]; Chang et al. [21]. This approach compresses the model information to its minimum, significantly reducing communication overhead and minimizing the sensitive information exposed. Using KD for model enhancement, Yuan et al. [48], enables KD-based FL to address non-IID challenges effectively. Moreover, even for traditional parameter-sharing algorithms, KD is a highly effective supplement that can contribute to client training, He et al. [81], and server aggregation stages, Zhang et al. [27].

## 3.2. How KD Tackles the Challenges of FL

3.2.1. Properties of KD. Due to the unique way of knowledge transfer, KD has some distinct properties that can help tackle the long-lasting challenges of FL as follows:

1. Model agnosticism: KD is model-agnostic, Tang et al. [51]. The student model can be designed with a personalized architecture based on actual needs without being required to be the same as the teacher model, Huang et al. [25]. This property makes personalization for clients in FL easy to achieve.
2. Model compression: KD was originally proposed to compress models for better production deployment, Hinton et al. [16]. In the classic KD architecture, there will be a large model as the teacher network and a small model as the student network. The purpose of training is to obtain a student network as small as possible, whose performance can meet actual needs.
3. Capacity gap: The teacher and student models may differ in capacity. The student model may be unable to learn more knowledge from a larger teacher model due to its own limitations, Cho and Hariharan [82]. Therefore, a larger teacher model may not always lead to better distillation results.
4. Model enhancement: The key to model compression is not just reducing model size but also ensuring that a smaller model can still have similar performance, Li et al. [83]; Phuong and Lampert [18]. Therefore, compressed models need to be enhanced to satisfy actual needs. KD can compress models by transferring dark knowledge while enhancing them.
5. Data dependence: KD is dependent on data, Hinton et al. [16]. The teacher model conveys knowledge to the student model by predicting the training data. Therefore, the quality of the training data has a significant impact on the distillation effect. In addition, training data can be unlabeled, Menghani and Ravi [84]; Lee et al. [85].
6. Efficient communication: Since only logits need to be transmitted, KD has good communication characteristics in distributed learning scenarios, Jeong et al. [29]. It enables collaboration between clients without transmitting complete model parameters.

Table 2 puts the properties mentioned above of KD and the challenges in FL to see the relationship between them. These properties give KD an advantage in solving certain challenges in FL.

Specifically, the approach of KD in perceiving and employing knowledge demonstrates unique potential in addressing challenges within the domain of FL:

ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License

Table 2: The relationship between KD properties and FL challenges.

| Personalization          | The model-agnostic nature of KD facilitates customer personalization, Afonin and Karimireddy [86]                                   | / When implementing model personalization, the capacity gap needs to be considered to choose an appropriate model architecture   | /                                                                                             | /                                                                                                                                                                                   | /                                                                                             |
|--------------------------|-------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------|
| Communication efficiency | / Clients can use compressed small                                                                                                  | models as intermediary models for transmission, Ozkara et al. [10] /                                                             | /                                                                                             | There is a communication burden when the shared dataset is large, Liu et al. [87]                                                                                                   | Only the logits of the model are transmitted to reduce communication costs, Jeong et al. [29] |
| Non-IID                  | The client can choose an appropriate model architecture based on local data, which helps alleviate non-IID issues, Shen et al. [41] | / /                                                                                                                              | The global model can learn personalized knowledge from the local model, which helps alleviate | non-IID issues, Zhang et al. [27] Depending on the correlation of the shared distillation dataset to the client dataset, the non-IID problem may be severe or mitigated, Liu et al. | /                                                                                             |
| Privacy preservation     | Tang The client's model architecture is unknown to other clients, helping to protect privacy, Gong et al. [34]                      | / /                                                                                                                              | /                                                                                             | Knowledge transfer relies on data and may compromise privacy, Gong et al. [34]                                                                                                      | Less information is transmitted, which helps protect data privacy, Gong et al. [34]           |
|                          | Model agnosticism, et al. [51] Model compression,                                                                                   | Hinton et al. [16] Capacity gap, Cho and Hariharan [82]                                                                          | Model enhancement, Phuong and Lampert [18]                                                    | Data dependence, Hinton et al. [16]                                                                                                                                                 | Efficient communication, Jeong et al. [29]                                                    |

- Privacy preservation: KD does not treat the model parameters as knowledge but the logits of the model as knowledge, effectively reducing privacy concerns associated with the potential leakage of model parameters, Geiping et al. [4]; Huang et al. [52]. This methodology aligns with the principle of data minimization, Biega et al. [88].
- Data heterogeneity: When dealing with non-IID data, KD distinguishes itself from direct parameter aggregation by utilizing implicit knowledge from diverse client models as mutual regularization constraints. Consequently, this mechanism prevents significant deviations among client models, He et al. [81]. This regularization constraint is more flexible than parameter aggregation, permitting clients to balance localized and global knowledge, Sattler et al. [20].
- Communication efficiency: KD maximizes efficiency in knowledge exchange by only sharing the model's outputs among various clients. Consequently, knowledge transmission improved efficacy. Compared to sharing model parameters, distributing model logits significantly reduces communication costs, Jeong et al. [29]; Ahn et al. [63].
- Personalization: As previously mentioned, KD treats the logits of the model as knowledge, which is independent of the specific model architecture, Tang et al. [51]. This characteristic empowers individual clients to design personalized model architectures tailored to their distinct requirements, Yamasaki and Takase [89]; Su et al. [90].

3.3. KD-Based FL: Taxonomy. According to the types of model information shared among clients, FD can be classified into three categories: feature-based FD, parameterbased FD, and data-based FD. Figure 5 shows the differences between these three types of FD. Feature-based FD shares model features, parameter-based FD shares model parameters, and data-based FD shares a local compressed data. Because the model information exchanged in these three FD methods is different, they exhibit different characteristics when dealing with different challenges in FL. Table 3 compares the three FDs in dealing with challenges in FL.

It is worth noting that this taxonomy is fundamentally different from traditional KD surveys, Gou et al. [47]; Wang and Yoon [46], which typically categorize methods based on the type of knowledge transferred (e.g., response-based, feature-based, and relation-based knowledge). Although this classification is insightful in centralized scenarios, it is not well-suited for FL due to its unique constraints and operational paradigm. In FL, what information is shared between clients in a secure and efficient way directly relates to privacy, communication overhead, and model heterogeneity. Our proposed classification method, based on the type of information shared between participants (features, parameters, or data), is specifically tailored for the FL scenario, whereas traditional KD classification can only generalize a portion of it, namely, feature-based FD. This perspective is directly linked to the core challenges of FL, providing a more functional, problem-oriented framework for analyzing KDbased FL algorithms.

## 3.3.1. Feature-Based FD (No Parameter Sharing)

Definition 3. In feature-based FD, clients share the model features rather than the model parameters, specifically by exchanging either the logits of the model output layer, Li and Wang [8], or intermediate representations, Gong et al. [31]. Feature-based FD is a classical algorithm in KD-based FL methods and has received significant attention in recent years due to its remarkable communication efficiency. Figure 6 illustrates the typical training process of feature-based FD.

1. Local logits uploading: Clients train their local models using private data and subsequently submit the logits of these models on a public dataset to the central server.
2. Global logits aggregation: The central server aggregates the logits from all clients and sends the average global logits back to each client.
3. Local model updating: Each client computes the distillation loss by utilizing the global average logits and subsequently updates their local model. The above steps are repeated until the stop condition is satisfied.

Advantages: Feature-based FD leverages the dark knowledge of client models to enhance the efficiency of client communication. Given the universality of this dark knowledge across different model architectures, Hinton et al. [16], clients can easily design heterogeneous model architectures customized to their personalized needs and effectively address challenges arising from system heterogeneity, Fang and Ye [93]; Li et al. [94]. The effective knowledge transfer method employed in KD enables clients with heterogeneous data distributions to learn from each other, offering a novel solution for mitigating non-IID problems, Huang et al. [95]; Itahara et al. [96]. In addition, the model's output logits are small, and the communication overheads, Jeong et al. [29]; Bistritz et al. [97], and privacy risks, K´ ad´ ar and Hadh´ azi [98]; Gong et al. [34], are minimal compared to conventional FL algorithms.

Disadvantages: KD relies on training data for knowledge transfer, and FL lacks access to clients' private data; alternative approaches are often necessary to acquire data for distillation, such as leveraging public datasets, Gong et al. [34]; Fang and Ye [93], or employing synthetic data generators, Li et al. [94]; Zhu et al. [99]. However, the availability of public datasets may be limited in certain scenarios, and collaborative training of generators among clients can impose additional computational costs. Therefore, exploring simpler and more effective methods for obtaining datasets suitable for distillation is crucial.

ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License

Figure 5: Comparison of the three KD-based FL methods. Feature-based FD shares model features, parameter-based FD shares model parameters, and data-based FD shares the local compressed dataset.

<!-- image -->

## 3.3.2. Parameter-Based FD (Sharing Parameters)

Definition 4. In parameter-based FD, clients are still required to share model parameters, but KD is utilized to address specific issues during the client training phase, Shen et al. [41], or server aggregation phase Zhang et al. [27]. This approach extends the capabilities of parameter-based algorithms by enabling more efficient model fusion and updates through the integration of KD. Figure 7 illustrates a typical training process of parameter-based FD as follows:

1. Local common model upload: Clients update the local common models using local private models and private datasets and then upload the common model to the central server.
2. Global common model aggregation: The central server aggregates the common models of all clients to obtain a new global common model and then sends it to each client.
3. Local common model update: Each client uses the global common model to perform KD for the local private model and then uses the updated local private model to distill into the common model.

Advantages: The parameter-based FD algorithm enhances traditional parameter-based FL algorithms. By introducing KD, the parameter-based FD approach can be seamlessly integrated with existing methodologies, demonstrating effectiveness across all stages of FL, Shang et al. [100]; He et al. [81]. Instead of simply averaging them, the server can efficiently aggregate diverse local models through dark knowledge fusion. Meanwhile, clients can leverage KD to acquire global knowledge while retaining personalized knowledge.

Disadvantages: Since the clients need to share model parameters, parameter-based FD has a large communication overhead. The process of distillation increases both the computational cost for clients and servers. For instance, achieving model heterogeneity in parameter-based FD settings requires transforming local personalized model architectures into a common architecture through KD, Ni et al. [101]; Ozkara et al. [10]. Furthermore, parameter-based FD also demands training data for distillation, similar to feature-based FD.3

## 3.3.3. Data-Based FD (Based on Dataset Distillation)

Definition 5. In data-based FD, clients employ dataset distillation, Wang et al. [38], to compress their local private data, resulting in a small-scale synthetic dataset. This dataset is then transmitted to the server for centralized training, Zhou et al. [42]; Song et al. [40]. Unlike traditional FL algorithms, databased FD does not share model information but rather shares local data (compressed) from the client. To ensure data privacy protection, data-based FD processes the local data so that the original data cannot be recovered. Figure 8 illustrates a typical training process of data-based FD as follows:

1. Local dataset distillation: Clients convert local datasets into small-scale compressed datasets through dataset distillation.
2. Local dataset upload: Clients upload the compressed datasets to the server.
3. Server centralized training: The server performs centralized training using compressed datasets uploaded by all clients.

Advantages: Clients transmit their compressed data directly to the server, facilitating subsequent training on the server without necessitating further client-server communication. This approach ensures efficient communication while addressing non-IID issues by replacing distributed learning with centralized learning, Zhou et al. [39]; Xiong et al. [102].

Disadvantages: The model's performance may decline when trained on the small-scale dataset obtained after dataset distillation, Wang et al. [38], owing to the inherent limitations associated with dataset distillation techniques. Moreover, direct transmission of local data raises significant privacy concerns. Consequently, further investigation is warranted to develop more efficacious and privacypreserving methodologies for dataset distillation.

ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License Table 3: Comparison between the three KD-based FL algorithms (feature-based, parameter-based, and data-based) on dealing with challenges in FL.

## Data-based,

## Parameter-based, Shen et al.

## Feature-based, Li and

## Zhou et al. [42]

## [41]

## Wang [8]

No need to share parameters, less privacy risk, Gong Need to share parameters, which poses privacy risks, Need to upload local data (compressed), which lead to privacy concerns Geiping et al. [4]

et al. [34]

Privacy Can effectively solve non-IID, Huang et al. [92]

Clients with heterogeneous data can learn from each

- other, mitigating non-IID, He et al. [81]

Clients with heterogeneous data can learn from each other, mitigating non-IID, Wen et al. [91]

Non-IID

- Communication costs are lower, Song et al. [40]
- Need to share parameters, high communication cost

No need to share parameters, low communication cost, Jeong et al. [29]

Communication Heterogeneous models are not supported Heterogeneous models are generally not supported Personalization Support heterogeneous models, Li and Wang [8]

Figure 6: The typical training process of feature-based FD: ① clients receive global average logits, ② clients use global average logits and public dataset to distill into the local models, ③ clients use the distilled models to continue training on the private dataset to obtain the updated models, ④ clients use the updated models to predict on the public dataset and obtain logits, ⑤ clients upload logits to the server, and ⑥ the server aggregates the logits of all clients to obtain the global average logits.

<!-- image -->

Figure 7: The typical training process of parameter-based FD: ① clients receive the global common model, ② clients use private datasets and the global common model to distill into local private models, ③ clients use private datasets and local private models and distill into common models, ④ clients upload the updated common models to the server, and ⑤ the server aggregates the common models of all clients to obtain a new global common model.

<!-- image -->

3.4. KD-Based FL: Comparison With Traditional FL. This section compares KD-based FL and traditional FL from multiple perspectives, as presented in Table 4.

The traditional FL is characterized by its simplicity in design and applicable to a wide range of client data distributions (suitable for horizontal FL, vertical FL [VFL], and federated transfer learning, Yang et al. [64]) and scenarios (suitable for cross-device and cross-silo, Karimireddy et al. [103]) without requiring additional public datasets. Parameter-based algorithms encompass diverse models in the parameter space, including linear models, tree models, neural network models, and recommendation algorithms, Zhang et al. [104]. However, the communication overhead associated with parameter sharing is relatively high, Luping et al. [70], and typically scales with the model's size. Since model parameters (weights) represent abstract dataset representations and gradients help identify landmarks for these representations, sharing parameters (or gradients) can lead to privacy leaks and security attacks, Geiping et al. [4]; Lam et al. [66]. Furthermore, parameter sharing is limited to client models with homogeneous architectures.

Figure 8: The typical training process of data-based FD: ① clients compress local datasets, ② clients upload compressed datasets, and ③ the server performs centralized training.

<!-- image -->

KD-based FL (specifically, feature-based FD) methods demonstrate a minimal communication overhead, which solely depends on the size of logits and remains unaffected by the model size. As there is no necessity to exchange or transmit model parameters (gradients), the associated privacy risks are notably mitigated. In KD, the student model does not need to be isomorphic to the teacher model, enabling each client's model to possess unique architectures, Ozkara et al. [10]. However, several relevant algorithms, Itahara et al. [96]; Gong et al. [31], based on distillation require clients to download a public dataset, rendering them unsuitable for cross-device scenarios with limited storage capacity. Moreover, performing distillation operations requires additional computational efforts from clients and servers. Typically, distillation-based approaches are better suited for neural network models.

3.5. KD-Based FL: Where Should KD Execute. For an FD system, it may be necessary to consider whether KD should be executed on the client or the server. KD involves two processes: (1) the collection of training data and (2) knowledge transfer. The collection of training data requires a certain storage capacity from the client. KD requires enough training samples to achieve good distillation results, Liu et al. [87], which is unsuitable for some client devices with limited storage capacity, such as downloading tens of thousands of images to a mobile phone with small storage space for distillation. The process of knowledge transfer may lead to increased computational overhead, which could pose a burden for devices with lower computing performance, particularly when extracting knowledge from a large model with tens of millions of parameters on low-performance internet of things (IoT) devices. Therefore, an FD algorithm that uses KD to solve the challenges of FL should also consider where KD is executed.

In FL, KD can be executed on (1) clients or (2) the server. Table 5 compares the advantages and disadvantages of executing KD on the client or server. The execution of KD on clients is generally more suitable for cross-silo, Huang et al. [13], scenarios due to the higher computational power of participants' computing devices. However, in the context of cross-device, Rehman et al. [105], scenarios, distillation training that needs a substantial amount of additional data and is time-consuming poses challenges due to the limited resources of client devices. When considering KD execution on a server, it is imperative to assess the availability of powerful servers. In traditional FL, the server only performs simple aggregation operations without engaging in resource-intensive tasks. However, if KD is conducted on the server, complexities arise, particularly when mutual distillation among diverse client models needs to be executed, Yang et al. [59]. This presents a significant challenge for server performance.

## 4. How KD-Based FL Tackles the LongLasting Challenges

In this section, we will analyze how KD-based FL addresses the long-lasting challenges of privacy protection, non-IID data, communication efficiency, and personalization. It is crucial to recognize that these challenges are not independent but are deeply intertwined, as illustrated in Figure 9. For instance, improving communication efficiency by transmitting less information (e.g., logits instead of parameters) inherently reduces the attack surface for privacy breaches, Li et al. [106]; Gong et al. [31]. Conversely, strong privacy-preserving techniques such as encryption can increase communication overhead. Similarly, the non-IID nature of data often slows down model convergence, Zhu et al. [99]; Mills et al. [107], necessitating more communication rounds, which in turn increases both communication costs and privacy risks. Finally, personalization offers a unified solution, as allowing clients to use custom, potentially smaller models can alleviate communication bottlenecks, mitigate non-IID issues by better fitting local data, and enhance privacy by obscuring a uniform global model architecture, Huang et al. [13]; Jing et al. [108]. Understanding these interdependencies is key to appreciating the holistic benefits of KD-based FL.

- 4.1. KD-Based FL for Privacy Protection
- 4.1.1. The Overview of Privacy Protection in KD-Based FL
- 4.1.1.1. Why Privacy Challenges Exist. While conventional FL algorithms were designed with privacy as a primary goal,

Table 4: Comparison of KD-based FL and traditional FL.

|               |                        | [57]                                            |                  |                                          |                                          |
|---------------|------------------------|-------------------------------------------------|------------------|------------------------------------------|------------------------------------------|
|               | [76]                   |                                                 |                  |                                          |                                          |
| al. [67]      | et al.                 | parameters, high communication cost, Koneˇ cn ỳ | dataset required | additional distillation process required | additional distillation process required |
| Need to share | causes serious non-IID | et al.                                          | al. [99]         | distillation                             | distillation                             |
| Yin et        |                        |                                                 | Zhu et           |                                          |                                          |
|               | Zhu                    |                                                 |                  |                                          |                                          |
|               |                        |                                                 | supported        |                                          |                                          |
|               | problems,              |                                                 |                  |                                          |                                          |
| risks,        |                        |                                                 |                  |                                          |                                          |
| privacy       |                        |                                                 | not              |                                          |                                          |
|               |                        |                                                 | are              |                                          |                                          |
| are           |                        |                                                 | models           |                                          |                                          |
| there         |                        |                                                 |                  |                                          |                                          |
|               |                        |                                                 |                  | no                                       | no                                       |
|               |                        |                                                 | public           |                                          |                                          |
| parameters,   |                        |                                                 | No               | itself,                                  | itself,                                  |
|               |                        |                                                 |                  | model                                    | model                                    |
|               |                        |                                                 | Heterogeneous    |                                          |                                          |
|               |                        |                                                 |                  | the                                      | the                                      |
|               | aggregation            |                                                 |                  |                                          |                                          |
|               |                        | share                                           |                  | train                                    | train                                    |
|               |                        | to                                              |                  |                                          |                                          |
|               | Direct                 |                                                 |                  |                                          |                                          |
|               |                        |                                                 |                  | Just                                     | Just                                     |
|               |                        | Need                                            |                  |                                          |                                          |
|               | Zhu                    | Ahn et al. [63]                                 |                  |                                          |                                          |
| et al. [21]   | non-IID,               |                                                 |                  |                                          |                                          |
|               |                        |                                                 | data,            |                                          |                                          |
|               |                        |                                                 | [8]              | knowledge                                | knowledge                                |
|               | mitigating             | cost,                                           | synthetic        |                                          |                                          |
| Chang         |                        |                                                 | Wang             |                                          |                                          |
| risk,         |                        |                                                 | and or           |                                          |                                          |
|               | other,                 |                                                 | Li [8],          | additional                               | additional                               |
|               |                        | communication                                   |                  |                                          |                                          |
| privacy       | each                   |                                                 | models,          | the                                      | the                                      |
| less          | from al. [99]          |                                                 | and Wang         | to                                       | to                                       |
|               | learn et               | low                                             |                  | due                                      | due                                      |
|               | can                    |                                                 |                  |                                          |                                          |
| parameters,   |                        |                                                 | heterogeneous Li | higher                                   | higher                                   |
|               | data                   | parameters,                                     |                  | is                                       | is                                       |
|               |                        |                                                 |                  | cost                                     | cost                                     |
|               |                        |                                                 | datasets,        |                                          |                                          |
| share         |                        | share                                           | Support          |                                          |                                          |
|               | heterogeneous          | to                                              | public           |                                          |                                          |
| to            |                        |                                                 |                  | computational                            | computational                            |
| No need       |                        | need                                            | requires         |                                          |                                          |
|               | with                   | No                                              |                  |                                          |                                          |
|               |                        |                                                 |                  | The                                      | The                                      |
|               |                        |                                                 | Usually          |                                          |                                          |
|               | Clients                |                                                 |                  |                                          |                                          |
|               |                        |                                                 |                  | costs                                    | costs                                    |
|               |                        |                                                 | dataset          |                                          |                                          |
|               | Non-IID                | Communication                                   |                  |                                          |                                          |
|               |                        |                                                 | Personalization  |                                          |                                          |
| Privacy       |                        |                                                 |                  | Computing                                | Computing                                |
|               |                        |                                                 |                  | Public                                   | Public                                   |

Table 5: Advantages and disadvantages of executing KD on the client or server.

| KD on the server   | Advantages Disadvantages does not need to capacity issues / reduce Computation bottlenecks occur when   | server can computation performing mutual distillation on large numbers of client nodes, Yang et al. [59] logits,   | transmit communication cost is small /                                                                               |
|--------------------|---------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------|
| KD on client       | Disadvantages Aburden for clients with limited storage capacity                                         | High computing performance requirements for clients When                                                           | logits on a public dataset need be transmitted to the server, the communication cost is substantial, Liu et al. [87] |
|                    | Advantages / reduce                                                                                     | Executing KD on the client can the pressure on server computation, He et al. [81]                                  | /                                                                                                                    |

Figure 9: The relationships between different challenges in FL. Increased communication helps alleviate non-IID but may lead to privacy concerns. Using too many privacy-preserving technologies can lead to a communication burden. Mitigating non-IID helps reduce communication. Personalization helps alleviate communication bottlenecks, privacy concerns, and non-IID.

<!-- image -->

the paradigm itself introduces complex security vulnerabilities. Recent studies, Geiping et al. [4]; Xu et al. [109], have revealed that sharing model parameters or gradients, even without direct data exchange, can expose sensitive client information. In fact, sensitive information from the client's training data may be contained in the local weights, aggregated weights, and the final model, Yin et al. [67]; Xu et al. [109]. The challenge is further magnified in modern applications such as the IoT and intelligent transportation systems, where FL is used for tasks such as anomaly detection or trust evaluation. In these scenarios, model updates could inadvertently leak patterns about industrial processes or user behavior, making robust privacy preservation not just a technical requirement but a critical safety and security concern, Wang et al. [110, 111]. The use of homogeneous models across clients exacerbates this risk, as it provides a consistent architecture for adversaries to exploit.

4.1.1.2. Conventional Methods for Privacy Challenges. Currently, there have been numerous studies, Mothukuri et al. [14], on privacy leakage in FL. Differential privacy (DP), Dwork [112], was used, Li et al. [113], to protect client data, but this resulted in reduced model performance. Truex et al. [114] combined DP with secure multiparty computation, Du and Atallah [115], enabling the federated system to mitigate the impact of noise as the number of participants increased while maintaining the expected level of trust without compromising privacy. However, this increased computation overhead. Yin et al. [67] investigated common privacy-preserving mechanisms in FL, including encryption-based privacy-preserving FL, Aono et al. [116]; Chen et al. [117, 118], perturbation-based privacypreserving FL, Hao et al. [119]; Hu et al. [120], anonymizationbased privacy-preserving FL, Xie et al. [121]; Choudhury et al. [122], and hybrid privacy-preserving FL, Xu et al. [123]; Truex et al. [114]. Recent efforts have also explored hierarchical FL architectures and federated reinforcement learning to enhance both security and quality of service (QoS) in complex environments such as the industrial IoT (IIoT) and intelligent transportation systems, Wang et al. [110, 111, 124], as well as generator-based adversarial learning for privacy-aware model training, Jiang et al. [125]. These methods are all based on parameter sharing and do not fundamentally address privacy concerns caused by the rich and redundant information carried by model parameters.

4.1.1.3. Why KD Can Address Privacy Challenge. In contrast to the rich and redundant information represented by model parameters, KD represents simplicity and efficiency. KD uses a method of sharing the logits of the model to transfer knowledge among clients. Unlike parameter sharing-based methods, in KD, different models provide regularization constraints to other models rather than the models themselves. This regularization constraint is implemented on the same dataset. Typically, in feature-based FD, clients exchange logits generated on the same dataset, greatly reducing communication, Chang et al. [21]; Li and Wang [8]. These logits act as regularizations for the local models, guiding the optimization direction of their training processes without the need to share more details of the local models, thereby protecting the privacy of local data. In, Gong et al., [31], the predictions and attentions of client models on a public dataset are uploaded to the server to train the global model, reducing communication overhead and protecting privacy. In addition, for parameter-based FD, although clients still need to share model parameters, privacy protection can still be achieved. For example, Wu et al. [126] use a common model for communication between local personalized models and the global model and transfer knowledge through KD between the local models and the common model, which enhances local data privacy protection to some extent.

4.1.2. KD-Based FL Methods for Privacy Challenge. Currently, some KD-based FL methods have been proposed to enhance data privacy.

In, Jeong et al., [29], each client periodically uploads the average logits of local categories (labels) on a private dataset, and the server aggregates the global average logits of each category. Then, each client utilizes the global average logits as the distillation regularization term for local loss. Furthermore, additional local training, Ahn et al. [63], is added based on this approach, Jeong et al. [29]. By uploading only the average logits of local labels, this method greatly protects the privacy of client data, as it does not require uploading model parameters. However, it restricts knowledge sharing among clients, which makes it challenging to ensure the effectiveness of the global model, particularly in non-IID scenarios, Jeong et al. [29].

Some research works, Huang et al. [95]; K´ ad´ ar and Hadh´ azi [98], utilize public datasets to achieve more diverse knowledge transfer. Specifically, Huang et al. [95] require each client to upload the logits of its local model on the public dataset and the cross-entropy on its private dataset. After receiving them, the server directly sends them back to each client. Then, each client uses the sum of the mean squared error (MSE) between the logits of other clients and their own logits as the distillation loss to update their local model (where the logits of each client are weighted based on their cross-entropy and similarity to the current client) and then fine-tunes on their local private data. In, K´ ad´ ar and Hadh´ azi, [98], each client uploads the logits of their local model on the public dataset to the server, and the server calculates the correlation matrix among clients using these ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License logits. Then, the server uses this matrix to calculate a regularized representation (logits) for each client, which is used for distillation among all clients.

Privacy leaks are usually caused by frequent communication between clients and servers, so many works, Gong et al. [34]; Zhang et al. [127], use the one-shot idea to greatly reduce the number of communication rounds. For example, Li et al. [106] divide local data into many parts, each of which is further divided into multiple subsets, and train a teacher model on each subset. These teacher models vote for the labels of unlabeled public data, obtaining the predicted labels for each public sample. A student model is then trained on the public data with predicted labels. Each client uploads its trained student models for each part to the server, which then uses these student models to predict the labels of each public sample (voting system). Finally, the model is trained on the public data with predicted labels. Although these methods still require uploading student model parameters, they improve communication efficiency and reduce the risk of privacy leakage since only one communication is needed.

In addition, commonly employed privacy protection techniques are utilized in the present study. Specifically, an unlabeled public dataset is used for distillation in, Sattler et al., [43]. During the preparation phase, each client trains a scorer to assign scores to the distillation samples. The training of the scorer incorporates DP, Dwork [128], to protect client privacy. To enhance privacy protection, DP is applied throughout the entire process in, Hoech et al., [129], which supplements the approach used in, Sattler et al., [43]. In, Gong et al., [34], quantization and noise integration are implemented on the logits shared among clients to further privacy protection.

4.1.3. Discussion of Privacy Protection in KD-Based FL. However, privacy concerns exist in KD-based FL, and the primary concern comes from the data needed for distillation. Since KD depends on data, an extra dataset is typically required for knowledge transfer in FD. Many studies, Chang et al. [21]; Li and Wang [8], use an additional public dataset and assume that it has the same distribution as the clients' local data, Li et al. [113]. This assumption can pose a risk of privacy leakage and lead to potential security threats. Some works, Zhu et al. [99]; Wen et al. [91], propose data-free distillation by collaboratively training a generator on the clients to generate synthetic data, eliminating the reliance on a public dataset. However, the process of collaboratively building the generator may still require parameter sharing, leading to privacy risks, Zhu et al. [65]. Moreover, the synthetic data generated by the generator may also leak clients' privacy data. A potentially feasible method is to ensure that the training data and original privacy data have the same knowledge properties but different representations, such as dataset distillation, Xiong et al. [102]; Song et al. [40].

Privacy-preserving techniques commonly used in FL, such as secure multiparty computation, Goldreich [130], homomorphic encryption, Yi et al. [131], and DP, Dwork [112], can still be applied in FD. For example, in, Sattler et al., [43], DP is used to update the local scorers and protect clients' privacy. Building on, Sattler et al., [43], Hoech et al. [129] extend the use of DP to the entire federated training process. These privacy-preserving techniques may increase communication overhead and have an impact on model performance. In practical use, finding a balance between privacy, communication, and performance is crucial.

Furthermore, the reliance on a public dataset, often assumed to be benign, introduces a new, subtle privacy attack surface. An adversary who controls or has knowledge of the public dataset could potentially infer information about a client's private data by analyzing how the client's logits on the public samples differ from the norm. The assumption that these public data are 'safe' and uncorrelated in a malicious way with private data is a strong one that needs careful validation in security-sensitive applications. Data-free methods using generators mitigate this, Zhang et al. [127], but as discussed, the generator itself becomes a potential source of privacy leakage.

- 4.2. KD-Based FL for Non-IID

4.2.1. The Overview of Non-IID in KD-Based FL

4.2.1.1. Why Non-IID Challenges Exist. In FL, the data among different clients are usually heterogeneous, which is non-IID, Zhu et al. [76]. This makes the optimization algorithms used in traditional machine learning and distributed learning unsuitable for FL. Each client uses local data for model updating, and the update directions of each client can be quite different from each other. Direct aggregation of the updates may not represent the globally optimal optimization direction and may even cause the global model to fail to converge. In addition, the amount of data owned by each client may vary significantly, which can result in clients with only a small amount of data having little weight in the aggregation, making the global model unfair. The non-IID in FL can be divided into two dimensions, Zhu et al. [76]: (1) spatial dimension and (2) temporal dimension. The spatial dimension non-IID is caused by different local data distributions among clients, while the temporal dimension non-IID is caused by different data distributions between new and old samples over time. The former can lead to client drift, Mendieta et al. [132], while the latter can lead to catastrophic forgetting, Shoham et al. [133]. Although there are conceptual differences between the two, they are both essential due to the deviation of the optimization direction caused by the difference in data distribution.

4.2.1.2. Conventional Methods for Non-IID Challenges. Currently, many studies, Zhu et al. [76], have been conducted on the non-IID problem in FL. Zhao et al. [5] indicate that the performance of the global model will significantly decrease in non-IID scenarios and propose to use a small subset of data that is globally shared to improve the performance of the global model. Sattler et al. [71] propose a compression framework called 'Sparse Ternary Compression (STC),' which extends the top-k gradient sparsity compression technique to achieve a higher downlink ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License communication compression rate in non-IID scenarios. Wang et al. [7] offset the model bias caused by non-IID by intelligently selecting appropriate clients to participate in each round of training. Zhu et al. [76] summarize the main approaches to handling non-IID data, including data-based approaches, Zhao et al. [5]; Duan et al. [134], algorithmbased approaches, Fallah et al. [135]; Arivazhagan et al. [136], and system-based approaches, Ghosh et al. [137]; Yin et al. [138].

4.2.1.3. Why KD Can Address Non-IID Challenge. Based on KD, FD alleviates non-IID problems from different perspectives through a special way of knowledge transmission. The main idea of FD for addressing non-IID problems is to guide models with different optimization directions to mutually influence each other within a certain range through dark knowledge, hoping that local models can learn personalized knowledge from other clients' models and reduce the overfitting problem, Nguyen et al. [37]; Wang et al. [139]. Similar to how FedProx, Li et al. [140], constrains the updates of local models during the local training stage, KDbased FL imposes a constraint on local model updates by aggregating the logits of diverse client models, thereby preventing excessive deviation of local models. In addition, KD can keep the local model stable, Liu and Yang [141]. Compared to parameter-based FL that directly replaces local models with the global model, which leads to the forgetting of historical personalized knowledge, Jin et al. [142], KD guides local model training in a more gentle way. Moreover, KD is an ideal way to implement model personalization in FL, allowing clients to share knowledge while designing heterogeneous models.

4.2.2. KD-Based FL Methods for Non-IID Challenge. In FL, the most direct solution for client drift, Karimireddy et al. [6], is to learn from other models, including mutual learning, Zhang et al. [143], between local models or between local and global models. In, Wu and Kwon, [144], Wasserstein distance, Panaretos and Zemel [145], and regularization terms are introduced into the objective function of federated KD to reduce the distribution difference between the global model and client models. Lee and Wu [146] propose a new model aggregation architecture to aggregate models by evaluating the effectiveness of deep neural networks (DNNs) and using KD and the uncertainty quantification method of DNN. In, Shang et al., [147], knowledge from the global model is used to guide local training to alleviate the local deviation, and local models are used to fine-tune the global model to reduce the volatility of training. In order to deal with heterogeneous tag sets in a multidomain environment, Wang et al. [148] propose a distillation method based on an instance weighting mechanism to facilitate cross-platform transfer of knowledge. In, Xing et al., [62], a teacher network on the client side teaches a student network using local data, and the student network is sent to the server. The server pairs the student models according to the minimum mean square distance and sends the paired models to each other as their respective teacher networks. In He et al. [81], an auxiliary dataset is used to evaluate the reliability of the global model on certain labels and dynamically adjust the weight of the distillation loss for each class during local training. Specifically, the server aggregates the client models to obtain the global model, and the server calculates the accuracy of the global model for each class using the auxiliary dataset and sends the accuracy to each client. The client optimizes the loss function (hard label cross-entropy + KD loss) using the local dataset, where the weight of the KD loss is related to the accuracy of each class. In, Yang et al., [59], the clients send their local models to the server, and the server conducts mutual distillation, Zhang et al. [143], (including features and logits) among all the local models using a labeled public dataset. In, Zheng et al., [149], clients use a self-distillation method to train local models. The server generates noise samples for each client and uses them to distill other client models.

Beyond simple logits transfer, some advanced methods leverage relational KD to better capture the complex data distributions inherent in non-IID settings, Han et al. [150]; Dong et al. [151]. Instead of matching the direct outputs, these methods enforce consistency in the relationships between data samples. For instance, FedRAD, Tang et al. [36], utilizes relational knowledge to help local models retain global knowledge more effectively, mitigating client drift by focusing on higher-dimensional data structures. This approach proves particularly effective because relational knowledge is often more robust to variations in data distribution than absolute output values, providing a more stable learning target for heterogeneous clients. Similarly, in federated incremental learning, relational KD can preserve semantic relationships between old and new classes, combating forgetting without requiring access to old data, Dong et al. [152].

Furthermore, traditional FD often relies on a single aggregated 'teacher' (e.g., averaged logits), which may perform poorly if a few clients with skewed data dominate the average. To address this, dynamic multiteacher distillation strategies have been proposed, Su et al. [153]. These methods move beyond simple aggregation and allow student models to learn selectively from a diverse set of teacher models (i.e., other clients). For example, FedMMD, Yang et al. [59], dynamically assigns multiple teachers for each student, allowing for more personalized and robust knowledge transfer by combining both intermediate representations and soft labels. This avoids the performance bottleneck of a single, averaged teacher and is particularly resilient to severe data heterogeneity. Other approaches, such as FedTAM, Mou et al. [154], employ attention mechanisms to allow a student client to weigh the importance of different teacher clients, thereby focusing on the most relevant knowledge and filtering out noise from dissimilar clients, improving system robustness against lowquality or even malicious participants.

Catastrophic forgetting, Halbe et al. [155], a core challenge in continual learning, manifests uniquely in FL and is tightly coupled with client drift. It occurs in two primary forms: (1) intertask forgetting, where learning a new global task erases knowledge from previous ones, and (2) intratask ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License forgetting, where a client's model, after adapting to its local data distribution, forgets the generalized global knowledge from the previous communication round. This latter form can be viewed as a direct consequence of client drift over time. KD provides a powerful mechanism to counteract this, acting as a dynamic regularization technique. By using a previous model (either a past local model or the global model) as a teacher, KD introduces a regularization term that penalizes deviations from previously learned knowledge representations. In essence, the distillation loss constrains the optimization trajectory of the local model, preventing it from shifting too far in response to local data. For instance, Ma et al. [156] employ client-side KD to address intertask forgetting and server-side KD to mitigate intratask drift among clients. Similarly, Tang et al. [36] use relational KD to better preserve global knowledge during local training. A critical challenge remains in quantitatively analyzing the relationship between the magnitude of local knowledge shift and the rate of global knowledge decay, which could inform the design of more adaptive regularization strengths in the distillation process. The authors of reference, Dong et al., [151] propose to learn a class-incremental model from a global and local perspective to mitigate catastrophic forgetting. To address global forgetting, the paper proposes a proxy server that assists in local relation extraction by selecting the best old global model.

The performance of the teacher model may be poor during the initial stage, which could potentially misguide the student model. Therefore, He et al. [81] use an auxiliary dataset to evaluate the credibility of the global model. In reference, He et al., [157], this selective learning is further developed in two aspects: (1) the local sample level, that is, the accuracy of the global model on a certain local sample, and (2) the class level, that is, the accuracy of the global model on a certain class. Specifically, the server evaluates the global model using the labeled auxiliary dataset to obtain a credibility matrix and sends it to each client. The client uses its local dataset to distill its local model (with the global model as the teacher). During distillation, the local model selectively learns by using a vector obtained from the credibility matrix, which is related to the current sample and relevant classes. The MSE loss function is obtained using this vector matrix as the distillation loss term, and cross-entropy is used as the loss term for the true labels. The two terms are added together to obtain the loss function, and the local model is updated through backpropagation and uploaded to the server.

Non-IID data may cause performance differences in the global model across different clients, leading to fairness issues, Noor et al. [158]. One approach to address this problem is to design personalized models for local data that participate in global model training through KD while maintaining performance on local data. In the literature, Ni et al. [101], the global common model is transformed into local personalized models through KD, which is then trained and transformed back to global common models. The personalized models can be uniquely designed based on the client's data distribution and device performance, and the differences between different models can be compensated through deep communication between the models. In the literature, Kim et al. [159], a novel hierarchical hybrid network is designed, which separates the local and global models into several blocks and connects them at different positions to form multiple paths, similar to a special type of intermediate layer distillation.

Existing FD methods generally do not take into account the diversity of all local models, which can lead to performance degradation of the aggregate model when some local models know little about the sample. In, Wang et al., [148], the local data of each client are regarded as a specific domain, and a novel domain-knowledge-aware FD method is designed to identify the importance of local models to distillation samples and optimize soft predictions from different models. Chen et al. [160] use entropy to determine the prediction confidence of each local model and select the most confident local model as a teacher to guide the learning of the global model.

4.2.3. Discussion of Non-IID in KD-Based FL. A significant number of FD studies, Huang et al. [30]; K´ ad´ ar and Hadh´ azi [98], operate on the crucial assumption that a common public dataset is available and that its distribution is sufficiently aligned with the clients' private data distributions. However, this assumption represents a major practical bottleneck and warrants critical examination. In many realworld scenarios, a perfectly correlated public dataset is either unavailable or difficult to procure. The inevitable domain shift, Luo et al. [161]; Sankaranarayanan et al. [162], between the public distillation data and private client data can severely impair the efficacy of knowledge transfer. If the distributions are misaligned, the 'knowledge' extracted from the public data may be irrelevant or even misleading for a client's local task, potentially exacerbating the non-IID problem rather than mitigating it. Therefore, developing FD methods that are robust to domain shift or that can perform effective knowledge transfer without a strongly correlated dataset remains a critical open research direction. Some initial progress has been made in cross-domain FL, Wang et al. [148,163], but the challenge is far from solved.

It is important to position KD-based approaches in the broader context of non-IID mitigation strategies. Many advanced algorithms tackle this challenge directly in the parameter space. For instance, FedProx, Li et al. [140], introduces a proximal term to the local loss function, which restricts local updates from deviating too far from the global model. SCAFFOLD, Karimireddy et al. [6], uses control variates to correct for 'client drift' by estimating the update direction of the global model and the average local model.

Compared to these parameter-based methods, KD-based FL offers several unique advantages. First, it is modelagnostic, naturally supporting heterogeneous client models, a scenario where methods such as FedAvg and FedProx struggle. Second, by operating in the function space (via logits) instead of the parameter space, KD can provide a 'softer' form of regularization that captures complex interclass relationships, which might be lost during direct parameter averaging. However, KD-based methods have ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License their own drawbacks, such as the reliance on a public dataset and the potential for domain shift, whereas methods such as SCAFFOLD operate without this requirement. The choice between these paradigms depends on the specific constraints of the FL application, such as model heterogeneity, data availability, and privacy requirements.

## 4.3. KD-Based FL for Communication Efficiency

## 4.3.1. The Overview of Communication Efficiency in KDBased FL

4.3.1.1. Why Communication Efficiency Challenges Exist. To facilitate collaboration among clients, FL requires periodic sharing of model parameters, which can lead to communication bottleneck problems. Communication efficiency is determined by two factors: the amount of information transmitted in each communication round and the number of communication rounds. Unfortunately, the performance of conventional parameter-based FL is inadequate in both aspects. First, sharing complete model parameters (or gradients) in each communication round can result in a large amount of data transmission, which is positively correlated with the size of the model architecture. This can make FL infeasible when using large model architectures. Second, the non-IID nature of client data in FL can make it difficult for the global model to converge, which can result in more communication rounds. The decline in communication efficiency can directly lead to a significant prolongation of the training time of the global model or even failure. In cross-device scenarios, the frequent joining and exiting of a large number of clients can further exacerbate the situation, leading to additional challenges for FL.

4.3.1.2. Conventional Methods for Communication Efficiency Challenges. Currently, numerous studies, Shahid et al. [68], have investigated communication efficiency issues in FL. Several methods have been proposed to reduce uplink communication costs, such as structured updates and sketched updates, Koneˇ cn ỳ et al. [57]. In addition, Luping et al. [70] introduced an orthogonal approach to identify irrelevant client updates, which can help to reduce communication costs. In, Sattler et al., [71], a STC framework was proposed to decrease the amount of data transferred in downlink communication. Another approach involves designing a probabilistic device selection scheme, Chen et al. [164], which facilitates the selection of high-quality clients in each communication round, as well as using quantization techniques to reduce the amount of communicated data. In, Lim et al., [165], common approaches for reducing communication costs were summarized. Although these methods have somewhat reduced the communication costs of downloading or uploading from clients, parameter sharing-based algorithms generally result in communication costs that are positively correlated with the model size.

4.3.1.3. Why KD Can Address the Communication Efficiency Challenge. Currently, traditional parameter-sharing FL

algorithms require frequent exchange of model parameters, leading to communication bottlenecks. KD uses the model's output instead of the model's parameters as the medium for knowledge propagation, facilitating communication between clients. Compared to model parameters, the model's output is much smaller, greatly reducing communication costs. Classic feature-based FD is mainly used to reduce the amount of data transmitted in a single communication round, and not the frequency of communication rounds. Therefore, some one-shot methods, Kang et al. [166]; Gong et al. [34], have been proposed to reduce the frequency of communication rounds. In addition, for parameter-based FD, although model parameters still need to be exchanged, communication costs can be greatly reduced because a smaller model can be obtained based on KD for communication between clients, Yao et al. [167].

4.3.2. KD-Based FL Methods for Communication Efficiency. FD was originally proposed to address the problem of the communication bottleneck, Jeong et al. [29]. Compared to FL algorithms based on parameter sharing, FD greatly reduces communication costs, Yang et al. [168]. As mentioned earlier, reducing communication costs involves two aspects: (1) reducing the amount of data transmitted in a single communication, and (2) reducing the rounds of communication. We will discuss these two aspects as follows.

First, FD can greatly reduce the amount of data transmitted in a single communication. Tanghatari et al. [169] use KDto exchange the knowledge learned by the server and the edge device. In order to reduce the communication overhead between the server and the edge device, we select the most valuable data to transfer. In, Jeong et al., [29], only the average logits of each client's local classes are uploaded, which is negligible compared to the model parameters. Similarly, in, Chen et al., [170], clients compute the mean representations of the data classes in their local training dataset and the corresponding mean soft prediction, which is sent to the server for aggregation through DP. However, these methods perform poorly in non-IID settings, such as scenarios where the same label has different features. Therefore, a more common method is to use a public dataset, Li and Wang [8], or synthetic data generated by a generator, Luo et al. [171]; Zhao et al. [172]. In, Sattler et al., [20], clients use a public dataset for distillation to obtain a distilled model and then use it to train local private data to obtain a local model. Then, the local model predicts soft labels on the public dataset, and the soft labels are quantized, encoded, and sent to the server. The server aggregates the soft labels sent by all clients to obtain the global soft labels and then distills and predicts the global model to obtain soft labels, which are quantized, encoded, and sent to clients. However, regardless of whether the distillation dataset is a public dataset or synthetic data, each client needs to share the output of its local model on the distillation dataset. When dealing with a large distillation dataset, the communication bottlenecks may still be an issue. In, Mo et al., [173], clients use a public dataset for the distillation of local models and then train on local data. Then, they predict soft labels on the ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License public dataset using the local model, as well as compressing the local control variables, which are sent to the server. The server decompresses the compressed data, aggregates soft labels and control variables, and then distills and predicts on the public dataset. Finally, the new soft labels and control variables are compressed and sent to the clients.

In addition, some works focus on reducing the number of communications. In, Zhang et al., [127], clients upload their local models to the server, which trains a generator using the integrated models of all clients. The generator then generates synthetic data, and the server uses the average logits of each client's model on the synthetic data (distillation loss) and the global model's cross-entropy loss on the synthetic data to train the model.

In recent years, an FL method based on dataset distillation, Wang et al. [38], has been proposed to address communication efficiency issues. Dataset distillation can compress large datasets into smaller ones while maintaining similar model performance. Inspired by dataset distillation, Zhou et al. [42] generate synthetic data for each class based on the parameters distributed by the server and send it to the server. The server uses the synthetic data to update the global model.

4.3.3. Discussion of Communication Efficiency in KD-Based FL. FD only needs to share logits without sharing model parameters, which seems promising and can greatly reduce communication costs. However, KD relies on data. FD algorithms require clients to share logits for all samples in a shared dataset, and usually, the larger the dataset, the better the performance, Liu et al. [87]. This can lead to additional communication costs, although these costs are much smaller than directly sharing model parameters. In some scenarios where a common dataset is not available, clients need to collaboratively train a generator to produce synthetic data for distillation, which incurs more computation and communication costs.

4.4. KD-Based FL for Personalization

4.4.1. The Overview of Personalization in KD-Based FL

4.4.1.1. Why Personalization Challenges Exist. In an FL system, clients provide the data, so their own needs should be fully respected. However, in parameter-based FL, clients have difficulty expressing their individual needs because all participants are required to train the same model, even if this model may not be suitable for some participants. This can lead to many problems in practice. On the one hand, participants are likely to refuse to join the FL system because it cannot satisfy their personalized needs, resulting in a smaller number of participants in the FL system and less available training data, which affects the effectiveness of model training. On the other hand, the non-IID problem in FL means that homogeneous models may not have performance advantages for some participants, resulting in fairness issues. In addition, for some clients with lower computational performance, it is difficult to train large federated models, while for some devices with limited bandwidth, it is difficult to bear heavy communication costs. Therefore, personalization is essential for the practical implementation of federated systems. Personalization can be reflected in two aspects: (1) personalization of model architecture and (2) personalization of model size. Clients can design customized model architectures, Gad and Fadlullah [174], based on the specific situations of their local data and the target application fields and can also design models of different sizes based on the computational performance and bandwidth resources of their devices.

4.4.1.2. Conventional Methods for Personalization Challenges. Currently, there are many studies, Kulkarni et al. [73], on personalization in FL. In, Arivazhagan et al., [136], a personalized layer method is proposed to alleviate the impact of non-IID. In, Deng et al., [175], an adaptive personalized FL is proposed, which combines global models and local models into a joint prediction model with an adaptive weight. In, Mansour et al., [72], three methods for achieving personalization are proposed, including user clustering, data interpolation, and model interpolation. Kulkarni et al. [73] and Tan et al. [9] summarize commonly used methods for model personalization.

4.4.1.3. Why KD Can Address Personalization Challenge. KD is model architecture agnostic, which means that the student model can use an architecture that is completely different from the teacher model. This property provides a natural advantage for achieving model personalization. In feature-based FD, clients can design personalized model architectures according to their own needs, as they only need to share logits that are independent of the model architecture. For parameter-based FD, KD can facilitate knowledge transfer between different model architectures. In this context, KD acts like a converter between models, transforming one into another. Therefore, local models can still be customized, and their knowledge can be transferred to the globally shared model architecture through KD for global aggregation.

4.4.2. KD-Based FL Methods for Personalization. Traditional FL algorithms require all clients to use homogeneous models, while FD allows clients to design heterogeneous models based on their actual needs to achieve personalization. The personalization of the model is mainly reflected in the size and architecture of the model. In conventional FL, clients may be passively or actively excluded due to the limitations of the global model's scalability for local training and deployment or its inability to meet specific business requirements. Different FL settings focus on different personalization, with cross-device scenarios focusing on model size personalization and cross-silo scenarios focusing on model architecture personalization. FD uses the model agnosticism of KD to enable models of different sizes and architectures to exchange knowledge with each other, Zhang et al. [176].

ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License Xu and Fan [177] propose a serverless framework that uses convolutional neural networks (CNNs) to accumulate common knowledge and KD to transfer it. Missing common knowledge is cycled through each federation to provide a personalized model for each group. Su et al. [178] introduce a dynamic selection algorithm that utilizes KD and weight correction to reduce the impact of model heterogeneity. Wang et al. [179] propose an effective federated graph learning method based on KD. Each client trains its own local model through KD. In, Zhu et al., [180], a progressive self-distillation, Zhang et al., [181] method is proposed for system heterogeneity and unstable network connections. To simply put, this method divides a large model by column, and each client can download a certain proportion of model parameters based on local network conditions and then supplement the missing parts with local models. Then, the complete local model is updated through progressive self-distillation. Then, based on the network condition, a certain proportion of local model parameters are uploaded. The server aggregates all the incomplete models uploaded by all clients to obtain the global model and broadcasts it to each client. In, Chen et al., [182], a decentralized FL system is designed using cyclic distillation to achieve knowledge accumulation. This method includes two stages: the public knowledge accumulation stage and the personalized stage. Both stages use a circular P2P architecture. In the public knowledge accumulation stage, clients decide whether to use distillation (with the previous client as the teacher) or directly use the previous client's model as the initialization of their local model based on the comparison of the current local model's accuracy and threshold. In the personalized stage, clients use the model and the comparison of the current local model's accuracy and threshold sent by the previous client to determine the weight of the distillation loss term. In, Liu et al., [183], models of different sizes are assigned to clients with different computing capabilities, with larger models being assigned to stronger clients and smaller models being assigned to weaker clients. A momentum KD method is also proposed to better transfer knowledge from the large models of stronger clients to the small models of weaker clients. In, Ozkara et al., [10]; Huang et al. [25], clients first use KD to learn from the global model locally to train a personalized model. Then, using KD, the personalized model is transformed into a globally common model, which is uploaded to the server for aggregation, Ozkara et al. [10], or to the other clients for distillation, Huang et al. [25]. Liu et al. [35] propose an adaptive quantization scheme for integrated distillation. This method divides clients into different clusters, and local models in the same cluster are isomorphic and have different quantization levels. Model aggregation includes single-cluster model aggregation and cluster-ensemble distillation loss aggregation.

Furthermore, advanced techniques such as relational KD can play a crucial role in personalization, as they allow heterogeneous models to align on the structural relationships within data, rather than on specific output values, providing a more flexible and powerful mechanism for knowledge sharing between diverse architectures, Tang et al. [36].

Personalization introduces a fundamental tension with global knowledge retention. A highly specialized local model, optimized exclusively for its own data distribution, inherently risks 'catastrophic forgetting' of the global consensus learned from all participants. This is where KD serves as an essential bridge. The process of distilling knowledge from a global model into a personalized local model is not just about improving performance; it acts as a continual learning regularizer, ensuring that as the local model specializes, it does not drastically deviate from the global knowledge. Thus, KD is the mechanism that allows personalization and global collaboration to coexist rather than conflict.

4.4.3. Discussion of Personalization in KD-Based FL. KD is model-agnostic, but certain drawbacks arise when employing it for personalized FL, which are inherent to the intrinsic characteristics of KD itself. Since KD is datadependent, achieving personalization requires the sharing of a common dataset globally, which poses a challenge for storage-limited clients. For instance, it is impractical to download a public dataset containing tens of thousands of images into a user's phone for distillation. Moreover, collecting a suitable public dataset is also a challenge, and in practical scenarios, such a dataset may not be available. In addition, KD increases additional computational costs. Specifically, for parameter-based FD, a KD is required before uploading the local model after receiving the global model, significantly increasing the computational cost for clients and prolonging the training time of the global model.

There is also a fundamental paradox in using a common public dataset to facilitate personalized models. While KD's model-agnostic nature allows for architectural freedom, the knowledge transfer is still anchored to the domain of this shared dataset. If the public data fail to capture the unique data distribution of a particular client, the resulting personalized model may be biased toward the public domain, thus undermining the goal of personalization.

## 5. Future Direction

Recently, KD-based FL has gained popularity as an FL paradigm, and numerous researchers have introduced KD into FL to tackle various challenges. However, this field is still nascent and faces several open challenges. Here are some promising future research directions:

1. Lightweight KD-based FL codesign for edge devices, Tanghatari et al. [169]: While KD can facilitate heterogeneous models, deploying large models on resource-constrained edge devices remains a challenge. Future work should focus on the codesign of lightweight distillation and FL protocols. This involves exploring how techniques such as model quantization and pruning can be integrated with FD to create highly efficient models that minimize both communication and computation, without significant performance loss. The goal is to design algorithms that are natively optimized for the heterogeneous hardware of edge ecosystems.

ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License

2. Advanced data-free distillation with generative models: The reliance on public datasets is a major limitation. While some works use generators, Zhang et al. [127]; Huang et al. [184], there is a need for more advanced data-free approaches. A key direction is leveraging generative adversarial networks (GANs), Goodfellow et al. [185], or diffusion models, Ho et al. [186], to synthesize high-fidelity data that capture the statistical properties of the clients' data without revealing private information. However, this introduces new challenges: ensuring the generated data do not inadvertently leak private information and managing the computational overhead of training complex generative models in a federated manner, Xu et al. [187].
3. Formal coupling of FD and DP, Dwork [112]: Applying DP to FD is a promising way to provide a formal privacy guarantee, Hoech et al. [129]. However, simply adding noise to logits can degrade utility. Future research should focus on the principled coupling and optimization of FD and DP. This includes theoretically analyzing the privacy-utility trade-off, designing novel noise-injection mechanisms tailored for distillation losses (e.g., influencing the temperature hyperparameter based on the privacy budget), and exploring how the choice of knowledge type (e.g., logits vs. intermediate features) impacts the overall privacy guarantee under DP.
4. Teacher-student role dynamics and credibility assessment: In FL, there is no pretrained, infallible teacher, Wang et al. [60]; Shao et al. [188]. Teacher models (whether global or peer clients) can be unreliable, especially in early training stages or under severe non-IID conditions. A critical direction is to develop dynamic mechanisms for assessing teacher credibility, He et al. [81], and selectively performing distillation. This could involve uncertainty estimation, where a student only learns from a teacher's predictions if the teacher's confidence is high, or designing adaptive weighting schemes that reduce the influence of potentially misleading teachers.
5. Theoretical understanding of knowledge compatibility: A core assumption in many FD methods is that knowledge (e.g., logits) from heterogeneous models is compatible and can be meaningfully aggregated, Chang et al. [21]; Li and Wang [8]. This assumption lacks rigorous theoretical backing. Future work should aim to develop a theoretical framework to understand and quantify knowledge compatibility between different model architectures in non-IID scenarios. This could help determine the upper bounds of performance for FD and guide the design of more effective aggregation strategies that account for potential knowledge conflicts.
6. Applications in VFL and incentive mechanisms: Most KD-based FL research focuses on horizontal FL. Exploring its application in VFL, Yang et al. [64], where clients share different feature spaces, is a largely

untapped area. KD could potentially be used to transfer knowledge across different feature domains, Huang et al. [189]; Gao et al. [190]. Furthermore, designing fair and effective incentive mechanisms for KD-based FL is challenging, as client contributions are obfuscated through logits on a public dataset, making it difficult to assess the value of their private data.

## 6. Conclusion

KD-based FL is an emerging FL paradigm that provides a series of new solutions to long-lasting challenges in FL by introducing KD. Given the lack of a comprehensive survey on the topic of KD-based FL in the current open literature, this paper provides a detailed survey of the application of KD in FL. First, we introduce the background of FL and KD in detail, especially how some properties in KD can be used to overcome some long-lasting challenges in FL. We then provide an overview of KD-based FL, discussing in detail the motivations, basics, taxonomy, comparison with traditional FL, and where should KD execute. We supplement several critical factors to consider in KD-based FL in the Appendix, including the teachers, knowledge, data, and methods. These critical factors are crucial to designing a practical KD-based FL algorithm. We survey and summarize existing KD-based FL methods and divide KD-based FL into feature-based FD, parameter-based FD, and data-based FD. Furthermore, we summarize how KD can be used to solve long-lasting challenges in FL, including privacy, non-IID, communication, and personalization. We also identify some open problems and future directions for further research in this emerging field.

Although current research on KD-based FL is still in its early stages, it is already generating significant interest. KD is expected to become an indispensable component in FL, bringing new possibilities to distributed learning. We hope that this survey paper can provide a useful reference for researchers and practitioners who are interested in KDbased FL and its applications.

## Appendix A: Critical Factors in KD-Based FL

Appendix A1: KD-based FL: Teachers: Using diverse models as teachers often means that the problems targeted are different in the FD algorithm. In FD, various teacher models exist, including local and global models, mutual teaching between models, and self-teaching by models themselves. The distinct identities of these teachers often result in varying learning objectives and teaching methods.

Local models as teachers: In FD, a prevalent approach is to consider local models as teacher models and the global model as a student model, Gong et al. [34]; Zhang et al. [27], which can be seen as a distributed multiteacher distillation, Wu et al. [191]. Treating local models as teachers offers several advantages. First, the global model can leverage the diverse and comprehensive knowledge from client models to enhance its generalization on unknown samples, Zhang et al. [27]. Second, in the feature-based FD setting, clients'

ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License knowledge is presented in a model-agnostic and consistent manner, Hinton et al. [16], facilitating the decoupling of local and global models at the architectural level and freeing local models from constraints imposed by identical architectures, Ozkara et al. [10]. Moreover, this allows for a better evaluation of each client model's contributions, which is an important consideration in FL systems with incentive mechanisms, Li et al. [192]. However, utilizing local models as teachers also entails drawbacks. First, while presenting logits of personalized client models in a unified form may seem feasible, their inner logic might be incompatible due to different representations of the same knowledge during feature extraction across clients, Wu et al. [193]. Directly aggregating client model knowledge could potentially lead to performance degradation of the global model. In addition, clients with higher weights will exert greater influence, Zhang et al. [27], on the global model's performance, thus making it more dependent on these clients, losing the benefits derived from the knowledge diversity offered by multiple clients.

Global model as a teacher: In FD, the global model can serve as a teacher model to guide the local models, He et al. [81]; Lee et al. [44], offering several advantages. First, the global model represents a fusion of diverse local models and theoretically exhibits superior generalization capabilities. Consequently, leveraging the global model as a teacher aims to enhance the generalization of local models, Lee et al. [44]; Mo et al. [173]. Second, FD facilitates knowledge transfer from the global model to local models through KD in a manner that respects personalization. This personalization is manifested in two aspects: (1) The global model incrementally improves the performance of local models without replacing them, thereby preserving locally personalized knowledge, Le et al. [194]; Ozkara et al. [10], and (2) the architecture of local models can differ from that of the global model, enabling personalization in terms of model architecture, Ozkara et al. [10]. However, employing the global model as a teacher also entails certain disadvantages. First, clients require additional computational resources for performing KD, which may pose challenges for devices with limited computing capabilities. Second, during the initial stages, when the performance of the global model might not be optimal yet, it could misguide local models.

Mutual teaching: Unlike traditional KD, FL typically does not have a pretrained high-performance teacher model, Wang et al. [60]; Shao et al. [188]. Instead, FL accumulates knowledge from different models through gradual mutual learning, Zhao et al. [195]; Li et al. [192]. Mutual learning between models offers several advantages. First, it mitigates the absence of a high-performance teacher model by enabling models to teach and learn from each other, enhancing generalization performance. Second, mutual learning allows models with different distributions to communicate effectively, effectively mitigating the non-IID issue, Taya et al. [22]; Ma et al. [196]. Moreover, the exchange of knowledge across diverse models and levels enhances the overall performance of the models. However, one major drawback of mutual learning is its increased computational burden on the entire system and frequent KD that significantly prolongs training time. Some algorithms, Yang et al. [59], perform mutual learning on servers; however, this approach may lead to the server becoming a bottleneck in the system.

Be my own teacher: Catastrophic forgetting, Dong et al. [151], is a significant challenge in FL, which occurs when a model forgets old knowledge after learning new knowledge, resulting in performance degradation of the original data distribution. Essentially, catastrophic forgetting can be viewed as a non-IID problem in the time dimension. Many FD algorithms, Ma et al. [156]; Yao et al. [197], address this issue by leveraging old models as teachers to guide new models, enabling them to learn new knowledge while retaining previous knowledge. In FL, model forgetting manifests in two main forms, Ma et al. [156]; Halbe et al. [155]: (1) intertask forgetting and (2) intratask forgetting. Intertask forgetting, Choi and Choi [198], means that a new FL task causes the model to forget the knowledge learned in the old task, while intratask forgetting, Ma et al. [156], means that the same FL task forgets the knowledge learned in the previous round of communication. To mitigate intertask forgetting, multiple historical models, Yao et al. [197], are typically employed as teacher models for better review of knowledge across different tasks. For addressing intratask forgetting, reviewing only the most recent old model, Ma et al. [156], from previous communication rounds is usually sufficient, since early models tend to have lower performance. Generally, local models do not need to review outdated global knowledge because local and global models continue communicating in subsequent updates. However, tackling model forgetting in FL presents certain challenges, such as increased storage costs due to storing historical models; this can be particularly challenging for clients with limited storage resources when multiple historical models are involved. In addition, reviewing historical models through KD incurs additional computational costs and necessitates retaining old data for improved review effects; however, this may not always be feasible due to privacy concerns and storage cost considerations.

Appendix A2: KD-based FL: Knowledge: For the student model, it is imperative not only to identify the teacher model but also to specify which knowledge should be acquired from it. A comprehensive discussion on the topic of the types of knowledge in KD can be found in, Gou et al., [47]; Chen et al. [199]. In this section, we will examine the characteristics of different types of knowledge under the framework of FL.

For simplicity, we classify knowledge into two categories: simple and complex types. It is evident that compared to simple knowledge, such as logits, Hinton et al. [16], complex knowledge, including intermediate features, Romero et al. [200]; Le et al. [201], and relationship features, Yim et al. [202]; Tang et al. [36], offers the student model a more comprehensive understanding. This becomes particularly crucial when addressing the challenge of non-IID in FL systems since complex knowledge can provide diverse insights from different models. However, it should be noted that an abundance of knowledge does not necessarily guarantee improved results; in extreme cases, the teacher model may transfer all its knowledge to the student model, which is equivalent to directly sharing model parameters. In ijis, 2025, 1, Downloaded from https://onlinelibrary.wiley.com/doi/10.1155/int/7406934 by Cochrane Philippines, Wiley Online Library on [24/05/2026]. See the Terms and Conditions (https://onlinelibrary.wiley.com/terms-and-conditions) on Wiley Online Library for rules of use; OA articles are governed by the applicable Creative Commons License