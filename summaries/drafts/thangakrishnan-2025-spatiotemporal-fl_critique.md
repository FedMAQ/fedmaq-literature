FIX: In the multi-objective scheduling objective (Section 3.6), the draft summary uses \(\max_{t \in T} L_{it}\), but the original paper's Equation (12) reads \(\max_{i \in T} L_{it}\). To exactly match the paper, the subscript should be \(i \in T\) (even though the paper likely contains a typo). The summary should reproduce the formula as given in the source, or explicitly note the correction. Otherwise, the summary is mathematically accurate, complete, and all other formulas and values are correct.